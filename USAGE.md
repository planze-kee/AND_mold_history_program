# 금형이력카드 처리 프로그램 - 사용 설명서

## 개요

HWP 형식의 금형이력카드에서 데이터와 이미지를 자동으로 추출하여 엑셀 DB로 관리하고, 템플릿 워드 문서에 자동 입력하는 통합 솔루션입니다.

v2.5부터 다크테마 UI가 적용되었으며, 탭이 실제 작업 순서(1→5단계)대로 배치됩니다.

---

## 시작하기

```bash
pip install -r requirements.txt
python main.py
```

### 시스템 요구사항
- Python 3.7+, Windows
- Microsoft Word 설치 (PDF 변환 기능 사용 시)

---

## 화면 구성

```
[AND 로고]  [★ 신규 이력카드 발행]
┌─ 공통 설정 ──────────────────────────────┐
│  DB 엑셀 파일 / Word 템플릿 / 이미지 폴더  │
└──────────────────────────────────────────┘
[1. HWP→엑셀] [2. 이미지 추출] [3. 문서 생성] [4. 이력 관리] [5. PDF]
...
작업 로그:                       [폴더 열기] [■ 취소]
```

### 공통 설정 (탭 위 영역)

여러 탭이 함께 사용하는 경로를 **한 곳에서 한 번만** 설정합니다.

| 항목 | 설명 | 사용하는 기능 |
|------|------|---------------|
| DB 엑셀 파일 | 마스터 DB XLSX | 문서 생성·동기화, 이력 관리, 신규 발행 |
| Word 템플릿 | placeholder가 포함된 양식 DOCX | 문서 생성·동기화, 이력 관리, 신규 발행 |
| 이미지 폴더 | 금형 사진 폴더 | 이미지 추출(출력), 문서 생성, 이력 관리 |

### 탭 구성 (작업 순서대로)

| 탭 | 기능 |
|----|------|
| 1. HWP→엑셀 | HWP → XLSX DB 변환 |
| 2. 이미지 추출 | HWP → 이미지 추출 (공통 이미지 폴더로 저장) |
| 3. 문서 생성 | XLSX → Word 일괄 생성, XLSX 변경 동기화 |
| 4. 이력 관리 | 유지보수 이력 기록·조회·Word 반영 |
| 5. PDF | DOCX → PDF 변환 및 병합 |

### 편의 기능

- **드래그&드롭**: 모든 경로 입력란에 탐색기에서 파일/폴더를 끌어다 놓을 수 있습니다.
- **폴더 열기**: 작업 완료 후 로그 우측 "폴더 열기" 버튼으로 출력 폴더를 탐색기로 엽니다.
- **취소**: 작업 중 "■ 취소" 클릭 시 현재 파일 처리 완료 후 안전하게 중단됩니다.
- **종료 보호**: 작업 실행 중 창을 닫으면 확인 후 중단·종료합니다.

### 키보드 단축키

| 단축키 | 동작 |
|--------|------|
| `Ctrl+R` / `F5` | 현재 탭의 실행 버튼 동작 |
| `Ctrl+L` | 작업 로그 지우기 |
| `Ctrl+Q` | 프로그램 종료 |

---

## 탭별 사용 방법

### 탭 1 — HWP→엑셀 (1단계)

HWP 파일들을 XLSX DB로 변환합니다.

1. 입력 폴더 설정 (HWP 파일들이 있는 폴더, 하위 폴더 포함 탐색)
2. 출력 XLSX 파일 경로 지정
3. "HWP → XLSX 변환" 클릭

**추출되는 필드**: 파일명, 보관회사명, 작성일자, 관리번호, 분류, 현 보관처, 제작처, MODEL명, 양산처, 품명, 도번번호, 금형규격, 금형재질(BASE/CORE), CAVITY수, 금형수명, GATE형식, 사용기계, 계약일, 승인일, 금형가

---

### 탭 2 — 이미지 추출 (2단계)

1. 입력 폴더 설정 (HWP 파일 폴더)
2. "이미지 추출" 클릭 — 결과는 **공통 설정의 '이미지 폴더'**에 저장됩니다

**파일명 규칙**: `品名_図番.jpg` (도번 없으면 `品名.jpg`, 이미지 여러 개면 `品名_2.jpg`)

---

### 탭 3 — 문서 생성 (3단계)

입력 파일(DB 엑셀·템플릿·이미지 폴더)은 **공통 설정**을 사용합니다.

**DOCX 일괄 생성**

1. 출력 폴더 지정
2. 처리 행 제한: `0` = 전체, `5` = 처음 5행만 (테스트용)
3. "DOCX 생성" 클릭

**동기화**

XLSX DB를 수정한 후 기존 Word 파일에 변경사항을 반영합니다.

1. "동기화 (파일명+내용 갱신)" 클릭
2. 변경된 행만 증분 재생성 (나머지는 스킵)
3. 템플릿 교체 시 자동 감지하여 전체 재생성 — 수동으로 강제하려면 "강제 전체 재생성" 체크

---

### 탭 4 — 이력 관리 (4단계)

유지보수 내역을 기록하고 Word 파일에 반영합니다. DB 엑셀·템플릿·이미지 폴더는 **공통 설정**을 사용합니다.

1. **Word 파일 폴더** 지정 (이력카드 DOCX가 있는 폴더)
2. **파일 목록**에서 대상 Word 파일 선택 — 목록 위 **검색란**으로 파일명 필터 가능
3. 이력 내용 입력 (텍스트 에디터)
4. **"저장"**: `.data/<연번>_history.txt` + XLSX `事 由` 컬럼 갱신
5. **"Word에 반영"**: 이력 포함하여 Word 파일 재생성 (이미지도 다시 탐색·삽입)

---

### 탭 5 — PDF (5단계)

| 모드 | 설명 |
|------|------|
| 단일 파일 변환 | 특정 DOCX 파일 → PDF (출력 경로 비우면 원본 폴더에 저장) |
| 일괄 변환 (폴더) | 폴더 내 전체 DOCX → PDF ("하위 폴더 포함" 체크 시 재귀 처리) |
| 변환 후 병합 | 목록의 Word 파일들을 PDF 변환 후 하나로 병합 |

- 병합 모드의 "개별 PDF 파일 저장 안 함" 체크 시 병합본만 남습니다.
- 변환 중 사용자가 열어둔 Word 문서에는 영향을 주지 않으며, 일괄 변환 시 Word 인스턴스를 재사용해 빠르게 처리합니다.

---

## 신규 이력카드 발행 (★ 버튼)

메인 화면 상단 "★ 신규 이력카드 발행" 버튼을 클릭합니다.
DB 엑셀은 공통 설정 값이 기본으로 입력되며, 다이얼로그에서 변경할 수 있습니다.

**다이얼로그 구성**

- **직전 항목 표시**: DB 마지막 행의 파일명과 품명 표시 (연번 확인용)
- **File name**: XLSX에서 자동 계산한 다음 연번 (직접 수정 가능)
  - 직접 수정하면 자동값으로 덮어쓰지 않으며, "↺ 자동값" 버튼으로 복원할 수 있습니다
- **필수 항목**: 품명, 도번번호, 관리번호
- **선택 항목**: 보관회사명, 작성일자, 승인일, 분류, 제작처, MODEL명, 양산처, 금형규격, CAVITY수
- **체크박스**: 新作 / 増作 / 二元化 / 業者更変 / 機種更変
- **금형 사진**: 이미지 파일 첨부 (선택)

"발행" 클릭 시:
1. XLSX DB에 새 행 추가
2. 출력 폴더(탭 3의 출력 폴더)에 Word 파일 생성 (`XX-NNN.docx`)
3. 사진 첨부 시 `.data/` 폴더에 이미지 복사

---

## 이미지 탐색 순서

Word 생성/재생성 시 아래 순서로 이미지를 탐색합니다:

1. XLSX `金型写真` 컬럼 값
2. `図番番号` 값
3. `品名` 값
4. 연번 (out_name)
5. `.data/` 폴더 (신규 발행 첨부 이미지)

각 단계에서 trailing underscore/hyphen 자동 제거 후 재탐색합니다.

---

## 템플릿 placeholder 목록

```
{保管会社名}   {作成日子}   {管理番号}   {分 類}    {現 保管処}
{製作処}       {MODEL名}    {量産処}     {品 名}    {図番番号}
{金型規格}     {金型材質-BASE}   {金型材質-CORE}   {CAVITY 数}
{金型寿命}     {GATE 型式}  {使用機械}   {契約日}   {承認日}
{金型価}       {新作}       {増作}       {二元化}
{業者変更}     {仕様変更}   {修理内訳}   {事 由}
{金型写真}     ← 이미지 자동 삽입 위치
```

---

## 설정 파일 (config.yaml)

경로 설정과 창 위치/크기가 자동 저장됩니다. 프로그램 종료 시 현재 값으로 갱신되므로 별도 수정 불필요합니다.

- 공통 경로 키: `db_xlsx`(DB 엑셀), `template`(Word 템플릿), `img_dir`(이미지 폴더)
- v2.5 이전 config.yaml의 구 키(`docx_xlsx`/`hist_xlsx`, `docx_template`/`hist_template`, `docx_img`/`hist_img`/`img_output`)는
  첫 실행 시 자동으로 통합 키로 마이그레이션됩니다 (`docx_*` 값 우선).

수동으로 수정하고 싶다면 프로그램 종료 후 텍스트 에디터로 편집하세요.

---

## 트러블슈팅

**Q: "입력 폴더를 찾을 수 없음"**  
→ "찾기..." 버튼으로 실제 경로를 다시 지정하거나, 탐색기에서 폴더를 입력란에 드래그하세요.

**Q: HWP 데이터가 일부 누락됨**  
→ HWP 양식이 표준 금형이력카드 형식인지 확인하세요. 필드 레이아웃이 다르면 파싱 오류가 발생할 수 있습니다.

**Q: Word 생성 후 이미지가 없음**  
→ 공통 설정의 이미지 폴더 경로가 올바른지 확인하고, 이미지 파일명이 `品名_図番.jpg` 또는 `品名.jpg` 형식인지 확인하세요.

**Q: 이력 저장 시 "XLSX 저장 실패"**  
→ 해당 XLSX 파일이 Excel 등 다른 프로그램에서 열려 있습니다. 닫고 다시 시도하세요.

**Q: PDF 변환 실패**  
→ Microsoft Word가 설치되어 있어야 합니다. Word를 설치하거나 다른 PC에서 실행하세요.

**Q: v2.5 업데이트 후 이력 관리 경로가 바뀌었음**
→ 구버전에서 이력 관리 탭에 별도 지정했던 DB/템플릿/이미지 경로는 공통 설정으로 통합되면서
   문서 생성 탭 값이 우선 적용되었습니다. 공통 설정에서 원하는 경로로 변경하세요.

---

**버전**: 2.5
**마지막 수정**: 2026-06-12
